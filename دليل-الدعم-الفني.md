# دليل الدعم الفني — محفظة جيب (NetCard Builder)

## على كل جهاز دعم فني

### المتطلب مرة واحدة فقط
ثبّت **Node.js LTS** من: https://nodejs.org

### التشغيل
1. فك ضغط الحزمة `NetCard-Builder-Support.zip`
2. انقر مرتين على **`INSTALL.bat`**
3. ينفّذ التثبيت تلقائياً ويفتح المتصفح على: **http://localhost:3000**
4. يُنشأ اختصار على سطح المكتب باسم **NetCard Builder** للاستخدام لاحقاً

للتشغيل لاحقاً: اختصار سطح المكتب أو `start.bat`

---

## إدخال البيانات

### أ) عميل واحد (بدون Excel)
املأ الحقول الثلاثة داخل البرنامج:
- اسم الشبكة
- رقم الكود
- رقم هاتف صاحب الشبكة

### ب) عدة عملاء (Excel)
ارفع ملف Excel من 3 أعمدة:
| اسم الشبكة | رقم الكود | رقم صاحب الشبكة |
|------------|-----------|------------------|
| شبكه انترنت | 12345 | 777465157 |

قالب جاهز مع المثال: `templates/قالب-بيانات-الشبكات.xlsx`  
أو من داخل البرنامج: **تحميل قالب Excel الجاهز**

---

## التصدير
بعد ضبط الليبلات على القالب اضغط التصدير — كل صورة تُنزَّل **باسم العميل**.

---

## حل المشاكل
| المشكلة | الحل |
|---------|------|
| Node.js غير موجود | ثبّت LTS من nodejs.org ثم أعد INSTALL.bat |
| فشل التثبيت | تحقق من الإنترنت ثم أعد INSTALL.bat |
| المتصفح لم يفتح | افتح يدوياً: http://localhost:3000 |
| لا تنزّل الصور | اسمح بتنزيلات متعددة في المتصفح |
